/**
 * first - last
 * 
 * 포함된 양 끝을 의미합니다.
 * 부터 ~~~ 까지
 * ex) dom에서 element.firstChild
 */

const students = ['포코', '존', '상윤'];

function getStudents(first, last){
    //...some code.
}

getStudents('포코', '현석');